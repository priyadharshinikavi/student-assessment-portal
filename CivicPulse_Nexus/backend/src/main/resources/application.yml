
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/civicpulse_db
    username: civicpulse
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
